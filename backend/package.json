{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon ./index.js localhost 5000",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "test:unit": "cross-env NODE_ENV=test jest --selectProjects unit --runInBand",
    "test:int": "cross-env NODE_ENV=test jest --selectProjects integration --runInBand",
    "test:watch": "cross-env NODE_ENV=test jest --watch --runInBand",
    "test:cov": "cross-env NODE_ENV=test jest --coverage --runInBand",
    "test:ci": "cross-env NODE_ENV=test jest --coverage --ci --runInBand --reporters=default"
  },
  "author": "Matheus <matheus@horadecodar.com.br> (http://www.horadecodar.com.br/)",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cookie-parser": "^1.4.5",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^6.0.6",
    "multer": "^2.1.1",
    "nodemon": "^2.0.12"
  },
  "devDependencies": {
    "cross": "^1.0.0",
    "cross-env": "^10.1.0",
    "env": "^0.0.2",
    "jest": "^30.4.2",
    "mongodb-memory-server": "^11.2.0",
    "supertest": "^7.2.2"
  }
}
